package cloudclub.blog.follow.controller;

import cloudclub.blog.posts.config.ResultMessage;
import cloudclub.blog.posts.dto.PostRequestsDto;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

@RestController
public class FollowInfoController {

    /** 아래 내용 사용 금지. 커밋만을 위함 */
//    @PostMapping("/v1/follow-info")
//    public ResponseEntity<ResultMessage> createFollowInfo(@RequestHeader Long userId) {
//        return new ResponseEntity<>(new ResultMessage(), HttpStatus.OK);
//    }
//
//    @GetMapping("/v1/follow-info")
//    public ResponseEntity<ResultMessage> getFollowList(@RequestHeader Long userId) {
//        return new ResponseEntity<>(new ResultMessage(), HttpStatus.OK);
//    }

}
